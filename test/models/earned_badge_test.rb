require 'test_helper'

class EarnedBadgeTest < ActiveSupport::TestCase
  # test "can exist without a task or submission" do
  #   EarnedBadge.create!(:student => student, :badge => badge)
  # end

  # test "can exist with a task but no submission" do
  #   EarnedBadge.create!(:student => student, :task => task)
  # end

  # test "can exist with a submission but no task" do
  #   EarnedBadge.create!(:submission => submission)
  # end

  # test "can exist with a task and a submission" do
  #   @submission = create_submission(:task => task)
  #   EarnedBadge.create!(:submission => @submission)
  # end

  # def assignment
  #   @assignment ||= create_badge
  # end
end

.small-12.panel.mainContent
  / Student Dashboard Breadcrumbs
  = content_nav_for current_student do
    = render "student_profile_tabs"

  .small-9.columns
    %h2
      Current Score:
      / Check to see if the course has a pre-determined total number of points
      - if current_course.point_total?
        %span#userScoreNum.bold #{points current_student.cached_score_for_course(current_course)}
        %span /#{points current_course.point_total} points
        / Else tally all of the points for all of the assignments in the course
      - else
        %span#userScoreNum #{points current_student.cached_score_for_course(current_course)}
        %span /#{points current_course.total_points} points
      - if current_student.grade_level_for_course(current_course).present?
        – Current Level:
        %span#userScoreNum= current_student.grade_level_for_course(current_course)

    = render 'students/syllabus'

    .clearfix

  = render "staff_view_student_sidebar"

  .clearfix



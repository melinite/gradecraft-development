.small-12.panel.mainContent
  / Badge Navigation
  = content_nav_for Badge, "New #{term_for(:badge)}" do
    = render "tabs"
  
  .hide-for-medium-up{:style => "padding: 10px"}
    = mobile_nav_for Badge, "New #{term_for(:badge)}" 

  .small-12.columns
    .hide-for-medium-up
      %a.radius.button.tiny.small-12.dropdown{:href => "#", :data => { :dropdown => "drop" } }
        Options
      %ul#drop.f-dropdown
        = render "tabs"
    %h2= @title
    = render 'form'

  .clearfix
.small-12.panel.mainContent
  / Assignment Type Navigation
  = content_nav_for AssignmentType, "New #{term_for :assignment}" do
    = render "tabs"

  .small-12.columns
    %h2= @title

    = render 'form'

  .clearfix

= content_nav_for AssignmentType, "Edit #{breadcrumb_link_for(@assignment_type)}".html_safe do
  %li= link_to "See All #{term_for :assignment} Types", assignment_types_path, :class => "button radius tiny"

%h2= @title
= render 'form'

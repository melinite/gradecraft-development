= content_nav_for Task do
  //%li= link_to 'New Task', new_assignment_task_path, :class => "btn btn-tiny btn-info"

%table.display.table-striped.simpleTable
  %thead
    %tr
      %th.sortable{"data-sort" => "string"} Title
      %th.sortable{"data-sort" => "string"} Description
      %th.sortable{"data-sort" => "string"} Due Date
      %th
      %th
      %th
  %tbody
    - for task in @tasks
      %tr
        %td= task.name
        %td= task.description
        %td= task.due_at
        //%td= link_to 'View', assignment_task_path(@assignment, task), :class => 'btn btn-tiny btn-success'
        //%td= link_to 'Edit', edit_assignment_task_path(@assignment, task), :class => 'btn btn-tiny btn-success'
        //%td= link_to 'Delete', assignment_task_path(@assignment, task), :class => 'btn btn-tiny btn-success', :data => { confirm: 'Are you sure?' }, :method => :delete

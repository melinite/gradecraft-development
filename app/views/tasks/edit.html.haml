= content_nav_for @assignment, "Editing #{(@task.name)}".html_safe do
  %li= link_to "See #{@assignment.try(:name)}", @assignment, :class => "btn btn-tiny btn-info"

.col-lg-12
  %h3= @title
  = render 'form'
= content_nav_for Assignment, "New #{term_for :assignment}" do
  %li= link_to "See All #{term_for :assignments}", assignments_path, :class => "button tiny radius"

.small-12
  %h2= @title
  = render 'form'


.small-12.panel.mainContent
  / Assignments Navigation
  .show-for-medium-up
    = content_nav_for Assignment, "#{@title}" do
      = render "tabs"

  .hide-for-medium-up{:style => "padding: 10px"}
    = mobile_nav_for Assignment, "#{@title}"

  .small-12.columns
    
    .hide-for-medium-up
      %a.radius.button.tiny.small-12.dropdown{:href => "#", :data => { :dropdown => "drop" } }
        Options
      %ul#drop.f-dropdown
        = render "assignments/tabs"
    
    = render 'form'

  .clearfix
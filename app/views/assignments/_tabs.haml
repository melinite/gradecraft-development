%li= link_to "See All #{current_course.assignment_term}s", assignments_path, :class => "button tiny radius"
%li= link_to "Create a New #{term_for :assignment}", new_assignment_path, :class => "button tiny radius"
%li= link_to "Create a New #{term_for :assignment_type}", new_assignment_type_path, :class => "button tiny radius"
%li= link_to "See All Ungraded Submissions", grading_status_path, :class => "button tiny radius"
- if @team.present? && @assignment.present?
  %li= link_to "Quick Grade #{@assignment.name}", mass_grade_assignment_path(@assignment, :team_id => @team), :class => "button tiny radius"
- elsif @assignment.present?
  %li= link_to "Quick Grade #{@assignment.name}", mass_grade_assignment_path(@assignment), :class => "button tiny radius"
- if @assignment.present?
  %li= link_to "Download Grades", export_grades_assignment_path(@assignment, format: 'csv'), :class => "button tiny radius"
  %li= link_to "See Standard Grade View", assignment_path(@assignment), :class => "button tiny radius"
  %li= link_to "Edit #{@assignment.name}", edit_assignment_path(@assignment), :class => "button tiny radius"

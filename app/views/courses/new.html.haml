.small-12.panel.mainContent
  / Courses Navigation
  .show-for-medium-up
    = content_nav_for @title do
      = render "tabs"

  .hide-for-medium-up{:style => "padding: 10px"}
    = mobile_nav_for @title

  .small-12.columns
    
    .hide-for-medium-up
      %a.radius.button.tiny.small-12.dropdown{:href => "#", :data => { :dropdown => "drop" } }
        Options
      %ul#drop.f-dropdown
        = render "tabs"

      = render 'form'

  .clearfix
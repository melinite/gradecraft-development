%form(method="post" action="#{@consumer.launch_url}" encType="application/x-www-form-urlencoded")
  - @consumer.generate_launch_data.each_pair do |key, val|
    %input(type="hidden" name=key value=val)
  %button(type="submit") Launch the tool

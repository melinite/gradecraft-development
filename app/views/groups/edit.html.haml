.small-12.panel.mainContent
  / Groups Navigation
  .show-for-large-up
    = content_nav_for Group, "Editing #{@group.name}" do
      = render "tabs"

  .hide-for-large-up{:style => "padding: 10px"}
    = mobile_nav_for @assignment, "Group Grade"

  .small-12.columns
  
    .hide-for-large-up
      %a.radius.button.tiny.small-12.medium-4.dropdown{:href => "#", :data => { :dropdown => "drop" } }
        Select
      %ul#drop.f-dropdown
        = render "tabs"

    %h2= @title

    %hr
    = render 'form'

  .clearfix
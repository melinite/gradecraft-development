%table.table.display.table-striped.simpleTable
  %thead
    %tr
      %th.sortable{"data-sort" => "string"} Username
      %th.sortable{"data-sort" => "int"} Current Score
      %th.sortable{"data-sort" => "string"} Final Grade
  %tbody
    - current_course.users.each do |user|
      %tr
        %td= user.username
        %td
          //= number_with_delimiter(user.earned_grades(current_course))
        %td
          //= current_course.grade_level(user)
= content_nav_for User do
  %li= link_to "See All #{term_for :students}", students_path, :class => "button tiny radius"
  %li= link_to "View #{@user.name}'s Dashboard", @user, :class => "button tiny radius"

.small-12
  %h2= @title
  = render 'form'

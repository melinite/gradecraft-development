.small-12.panel.mainContent
  / New user navigation
  .show-for-large-up
    = content_nav_for User, "Create a New User" do
      = render "tabs"
  
  .hide-for-large-up{:style => "padding: 10px"}
    = mobile_nav_for User, "Create a New User" 

  .small-12.columns

    .hide-for-large-up
      %a.radius.button.tiny.small-12.medium-4.dropdown{:href => "#", :data => { :dropdown => "drop" } }
        Select
      %ul#drop.f-dropdown
        = render "tabs"

    / Form for creating a new user
    = render 'form'

  .clearfix
- if @user.submission_for_assignment(assignment)
  = link_to "See Submission", assignment_submission_path(assignment, @user.submission_for_assignment(assignment), :student_id => @user), :class => 'btn btn-tiny btn-info'
- else
  = link_to "Submit", :class => 'btn btn-tiny btn-success'
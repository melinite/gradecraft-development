= content_nav_for @assignment, "Grade" do
  %li= link_to "All #{@grade.assignment.name } Grades", assignment_path(@grade.assignment_id), :class => "btn btn-tiny btn-info"
  - if @assignment.is_individual?
    %li= link_to 'Edit Grade', edit_assignment_grade_path(@assignment, :student_id => @grade.student_id), :class => "btn btn-tiny btn-info"
    %li= link_to "Delete Grade", assignment_grade_path(@assignment, :student_id => @grade.student_id), :class => "btn btn-tiny btn-info", :data => { confirm: 'Are you sure?' }, :method => :delete
    //%li= link_to "Grade Another " +@grade.assignment.name, new_assignment_grade_path("assignment_id"=>@grade.assignment.id), :class => "btn btn-tiny btn-info"
  - else
    %li= link_to 'Edit Grades', group_grade_assignment_path("assignment_id" => @assignment.id, "id" => @grade.id, :group_id => @grade.group_id), :class => "btn btn-tiny btn-info"
    //%li= link_to "Grade Another " +@grade.assignment.name, new_assignment_grade_path("assignment_id"=>@grade.assignment.id), :class => "btn btn-tiny btn-info"


.col-lg-12.info
  %h3= @grade.assignment.name
  - if @grade.assignment.is_individual?
    = render "individual_show"
  - else
    = render "group_show"

//initiating jQuery
!function($) {
  var $document = $(document);
    //enabling stickUp on the '.navbar-wrapper' class
    $('.gradePredictor').stickUp({
    marginTop: 'auto'
    });

  init();
}(jQuery);